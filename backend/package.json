{
  "name": "camp-alloc-backend",
  "version": "3.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --experimental-vm-modules node_modules/.bin/jest --config jest.config.js",
    "test:watch": "node --experimental-vm-modules node_modules/.bin/jest --watch --config jest.config.js"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "jest": "^29.7.0",
    "wrangler": "^3.0.0"
  }
}
